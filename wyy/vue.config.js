// vue.config.js  导出的是一个对象
module.exports = { // 选项
    // 基本路径
    publicPath: "./",
    // 构建时输出目录
    outputDir: "dist",
    // 放置静态资源的目录  
    // assetsDir:"",
    // 指定生成的 index.html 的输出路径（相当于outputDir）,也可以是一个绝对路径
    // indexPath: "index.html",
}